const generateMeetingLink = () => {
    // Return a unique meeting link (Example: using a random string or timestamp)
    return `https://meet.example.com/${Date.now()}`;
};

export { generateMeetingLink };


